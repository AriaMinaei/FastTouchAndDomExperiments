var define;

if (typeof define !== 'function') {
  define = require('amdefine')(module);
}

define(function() {
  var Frames;

  return Frames = {
    requestUnDroppableFrame: function(callback) {},
    requestUnDroppableStream: function(callback) {
      return streamId;
    },
    cancelDroppableStream: function(streamId) {}
  };
});

/*
//@ sourceMappingURL=frames.map
*/
